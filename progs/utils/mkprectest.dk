# Create a test case to exercise operator precedence



mproc permutations_for {
	seq
	sub 1 func
} {
	if (seq.size <= 1) {
		return [func seq]
	}

	var first = seq@0
	var rest = [slice 1 seq.last]

	permutations_for rest {
		var instance = [slice a 1 nil]
		for index in (0 .. a.last) {
			# XXX
		}
	}


}